import { FilterPartsByAppliancePipe } from './filter-parts-by-appliance.pipe';

describe('FilterPartsByAppliancePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPartsByAppliancePipe();
    expect(pipe).toBeTruthy();
  });
});
